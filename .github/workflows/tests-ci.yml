name: BugBank CI

on: [push]

jobs: 
  cypress-electron-tests:
    runs-on: ubuntu-latest
    steps: 
      - name: Get code
        uses: actions/checkout@v3.5.3

      - name: install dependencies
        run: npm install

      - name: Run e2e tests
        run: npx cypress run

      
